# 📋 دليل نظام الجلسات التعاونية (Collaborative Sessions)

## 🎯 نظرة عامة

نظام الجلسات التعاونية هو ميزة متقدمة تتيح للمستخدمين العمل معاً على مجموعة من الطرود في الوقت الفعلي. يمكن لعدة مستخدمين الانضمام لنفس الجلسة ومشاهدة التحديثات الفورية وتعديل الطرود بشكل تعاوني.

---

## ✨ الميزات الرئيسية

### 1. **إنشاء الجلسات**
- إنشاء جلسة عمل جديدة من الطرود المفلترة
- تحديد اسم ووصف للجلسة
- اختيار الطرود المراد مشاركتها (فلتر حالي أو تحديد يدوي)
- دعوة مستخدمين عبر البريد الإلكتروني

### 2. **نظام الصلاحيات**
- **مالك (Owner)**: صلاحيات كاملة - إنشاء، تعديل، حذف، إدارة المشاركين
- **مدير (Admin)**: إدارة المشاركين، تعديل الطرود، دعوة مستخدمين
- **محرر (Editor)**: تعديل الطرود وتغيير الحالات
- **مشاهد (Viewer)**: عرض الطرود فقط بدون تعديل

### 3. **المزامنة الفورية**
- تحديثات فورية لجميع المشاركين عند أي تغيير
- عرض المستخدمين النشطين في الوقت الفعلي
- نظام قفل الطرود لمنع التعديلات المتزامنة
- إشعارات فورية عند التحديثات

### 4. **تتبع النشاطات**
- سجل كامل لجميع التغييرات
- معرفة من قام بماذا ومتى
- تتبع حالة المزامنة

### 5. **الإحصائيات**
- إحصائيات شاملة للجلسة
- أداء كل مشارك (عدد الطرود المسلمة، التحديثات)
- نسبة الإنجاز

---

## 🚀 كيفية الاستخدام

### إنشاء جلسة جديدة

1. **افتح صفحة الجلسات**
   - من القائمة الرئيسية، اضغط على "الجلسات التعاونية"

2. **أنشئ جلسة جديدة**
   - اضغط على زر "جلسة جديدة"
   - أدخل اسم الجلسة (مثل: "توصيل حي السلام - 22 مارس")
   - أضف وصفاً اختيارياً

3. **اختر الطرود**
   - **الفلتر الحالي**: يتم مشاركة جميع الطرود المفلترة حالياً
   - **تحديد يدوي**: اختر الطرود يدوياً

4. **ادعُ المستخدمين**
   - أدخل البريد الإلكتروني للمستخدم
   - اختر الدور (مشاهد، محرر، مدير)
   - اضغط على "إضافة"
   - يمكنك دعوة عدة مستخدمين

5. **أنشئ الجلسة**
   - اضغط على "إنشاء الجلسة"
   - سيتم إرسال دعوات للمستخدمين المدعوين

### الانضمام لجلسة

1. **من قائمة الجلسات**
   - افتح صفحة الجلسات
   - ستظهر لك قسم "دعوات الانضمام" في الأعلى إذا كان لديك دعوات معلقة
   - اضغط على "قبول الدعوة" للانضمام مباشرة
   - أو "رفض" لإزالة الدعوة من قائمتك

2. **الجلسات المشترك فيها**
   - الجلسات التي قبلت دعوتها أو أنشأتها ستظهر في قسم "الجلسات النشطة"
   - اضغط على أي جلسة للدخول إليها والبدء بالعمل التعاوني.

### العمل داخل الجلسة

#### عرض الطرود
- جميع الطرود المشتركة تظهر في القائمة
- يمكنك رؤية من يعمل على أي طرد حالياً
- الطرود المقفلة تظهر بشارة حمراء

#### تعديل الطرود
1. اضغط على الطرد المراد تعديله
2. سيتم قفل الطرد تلقائياً لمنع التعديلات المتزامنة
3. قم بالتعديلات المطلوبة
4. احفظ التغييرات - سيتم فك القفل تلقائياً

#### عرض المشاركين
- في أعلى الصفحة، يمكنك رؤية جميع المشاركين
- النقطة الخضراء تعني أن المستخدم نشط حالياً
- يمكنك رؤية دور كل مشارك

#### سجل النشاطات
- اضغط على زر "السجل" (أيقونة الساعة)
- شاهد جميع التغييرات التي حدثت
- معرفة من قام بماذا ومتى

#### الإحصائيات
- اضغط على زر "الإحصائيات" (أيقونة الرسم البياني)
- شاهد إحصائيات شاملة للجلسة
- أداء كل مشارك

### إدارة الجلسة

#### دعوة مستخدمين جدد
1. اضغط على زر "دعوة" (أيقونة +)
2. أدخل البريد الإلكتروني والدور
3. اضغط على "إرسال الدعوة"

#### تعديل دور مشارك
1. من قائمة المشاركين
2. اضغط على المستخدم
3. اختر الدور الجديد

#### إزالة مشارك
1. من قائمة المشاركين
2. اضغط على زر الحذف بجانب المستخدم
3. أكد الإزالة

#### إنهاء الجلسة
1. من قائمة الجلسات
2. اضغط على زر القائمة (...)
3. اختر "إنهاء الجلسة"
4. الجلسة ستنتقل للأرشيف

#### حذف الجلسة
1. من قائمة الجلسات
2. اضغط على زر الحذف (أيقونة سلة المهملات)
3. أكد الحذف
4. **تحذير**: لا يمكن التراجع عن هذا الإجراء

---

## 🔧 التكامل التقني

### الملفات المضافة

#### 1. `assets/js/sessions.js`
ملف JavaScript الرئيسي لإدارة الجلسات، يحتوي على:
- دوال إنشاء وإدارة الجلسات
- نظام الصلاحيات
- المزامنة الفورية مع Firestore
- نظام الحضور (Presence System)
- سجل النشاطات

#### 2. `sessions-ui.html`
واجهة المستخدم الكاملة للجلسات، تحتوي على:
- صفحة عرض الجلسات
- نافذة إنشاء جلسة جديدة
- نافذة تفاصيل الجلسة
- نوافذ الإحصائيات والنشاطات

#### 3. تحديثات `assets/js/state.js`
إضافة متغيرات الحالة للجلسات:
- بيانات الجلسات
- المشاركون
- الطرود المشتركة
- حالة المزامنة

#### 4. تحديثات `assets/js/methods.js`
إضافة دوال Vue.js للجلسات:
- `openSessionsView()`
- `createSession()`
- `joinSession()`
- `leaveSession()`
- وغيرها...

#### 5. تحديثات `assets/css/style.css`
أنماط CSS مخصصة للجلسات:
- تأثيرات بصرية
- أنماط البطاقات
- تأثيرات الانتقال
- استجابة للشاشات الصغيرة

### هيكل البيانات في Firestore

```
sessions/
  {sessionId}/
    - id
    - name
    - description
    - createdBy
    - createdAt
    - status
    - filters
    - stats
    - settings
    
    participants/
      {userId}/
        - email
        - role
        - isOnline
        - lastSeen
        - stats
    
    parcels/
      {parcelId}/
        - [بيانات الطرد]
        - lastModifiedBy
        - lockedBy
    
    activity/
      {activityId}/
        - type
        - userId
        - timestamp
        - details
```

---

## 📝 خطوات التثبيت

### 1. استيراد ملف sessions.js

أضف هذا السطر في `index.html` قبل إغلاق `</body>`:

```html
<script type="module">
    import sessionsManager from './assets/js/sessions.js';
    window.sessionsManager = sessionsManager;
</script>
```

### 2. دمج واجهة المستخدم

انسخ محتوى `sessions-ui.html` والصقه في `index.html` في المكان المناسب:
- زر الجلسات في القائمة الرئيسية
- صفحة عرض الجلسات
- النوافذ المنبثقة

### 3. تحديث قواعد Firestore

أضف هذه القواعد في Firebase Console لضمان عمل المزامنة والاستعلام بكفاءة:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد الجلسات
    match /sessions/{sessionId} {
      // السماح بالقراءة للمشاركين فقط
      // تدعم القراءة الفردية والاستعلام عن القائمة
      allow read: if request.auth != null && 
        (request.auth.uid in resource.data.participantIds || 
         exists(/databases/$(database)/documents/sessions/$(sessionId)/participants/$(request.auth.uid)));
      
      // السماح بالإنشاء لأي مستخدم مسجل
      // السماح بالتعديل والحذف للمالك فقط
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && 
        resource.data.createdByUid == request.auth.uid;
      
      // المشاركون
      match /participants/{userId} {
        allow read: if request.auth != null;
        allow write: if request.auth != null && 
          (request.auth.uid == userId || 
           get(/databases/$(database)/documents/sessions/$(sessionId)/participants/$(request.auth.uid)).data.role in ['owner', 'admin']);
      }
      
      // الطرود
      match /parcels/{parcelId} {
        allow read: if request.auth != null;
        allow write: if request.auth != null && 
          get(/databases/$(database)/documents/sessions/$(sessionId)/participants/$(request.auth.uid)).data.role in ['owner', 'admin', 'editor'];
      }
      
      // قواعد النشاطات
      match /activity/{activityId} {
        allow read: if request.auth != null && 
          (request.auth.uid in get(/databases/$(database)/documents/sessions/$(sessionId)).data.participantIds ||
           exists(/databases/$(database)/documents/sessions/$(sessionId)/participants/$(request.auth.uid)));
        
        allow create: if request.auth != null;
      }
    }

    // 3. قواعد الدعوات العامة
    match /invites/{inviteId} {
      // السماح بالقراءة فقط إذا كان البريد الإلكتروني للمستخدم يطابق الدعوة
      // أو إذا كان المستخدم هو من أرسل الدعوة
      allow read: if request.auth != null && 
        (request.auth.token.email.toLowerCase() == resource.data.email || 
         request.auth.uid == resource.data.invitedByUid);
      
      // السماح بالإنشاء لأي مستخدم مسجل
      allow create: if request.auth != null;
      
      // السماح بالتعديل (قبول/رفض) للمدعو
      allow update: if request.auth != null && 
        request.auth.token.email.toLowerCase() == resource.data.email;
    }
  }
}
```

---

## 🎨 التخصيص

### تغيير الألوان

في `assets/css/style.css`، يمكنك تعديل ألوان الأدوار:

```css
.role-owner {
    background: linear-gradient(135deg, #a855f7 0%, #7c3aed 100%);
}

.role-admin {
    background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
}

.role-editor {
    background: linear-gradient(135deg, #10b981 0%, #059669 100%);
}

.role-viewer {
    background: linear-gradient(135deg, #6b7280 0%, #4b5563 100%);
}
```

### تعديل مدة القفل

في `assets/js/sessions.js`، يمكنك تعديل مدة قفل الطرد:

```javascript
settings: {
    lockTimeout: 120000 // 2 دقيقة (بالميلي ثانية)
}
```

### تخصيص الإشعارات

في `assets/js/methods.js`، يمكنك تعديل دالة `showSessionNotificationToast()`:

```javascript
showSessionNotificationToast(message) {
    if (this.settings.notifyOnChanges !== false) {
        this.showToast(message, 'info');
        // يمكنك إضافة صوت أو اهتزاز هنا
    }
}
```

---

## 🔒 الأمان

### نصائح الأمان

1. **استخدم قواعد Firestore الصحيحة**
   - تأكد من تطبيق القواعد المذكورة أعلاه
   - لا تسمح بالوصول العام للجلسات

2. **التحقق من الصلاحيات**
   - جميع العمليات تتحقق من صلاحيات المستخدم
   - لا يمكن للمشاهدين تعديل الطرود

3. **نظام القفل**
   - يمنع التعديلات المتزامنة
   - يتم فك القفل تلقائياً بعد مدة محددة

4. **تشفير البيانات**
   - جميع البيانات محمية بـ Firebase Authentication
   - الاتصال مشفر عبر HTTPS

---

## 🐛 استكشاف الأخطاء

### المشاكل الشائعة

#### 1. لا تظهر الجلسات
**الحل:**
- تأكد من تسجيل الدخول
- تحقق من قواعد Firestore
- افتح Console وابحث عن أخطاء

#### 2. لا تعمل المزامنة الفورية
**الحل:**
- تأكد من الاتصال بالإنترنت
- تحقق من استيراد `sessions.js` بشكل صحيح
- راجع Console للأخطاء

#### 3. لا يمكن تعديل الطرود
**الحل:**
- تأكد من دورك (يجب أن تكون محرر أو أعلى)
- تحقق من أن الطرد غير مقفل من مستخدم آخر
- انتظر دقيقتين إذا كان مقفلاً

#### 4. لا تصل الدعوات
**الحل:**
- تأكد من صحة البريد الإلكتروني
- تحقق من صندوق البريد المزعج
- حالياً الدعوات تُسجل في Firestore فقط (يمكن إضافة إرسال بريد لاحقاً)

---

## 📊 الأداء

### نصائح لتحسين الأداء

1. **حدد عدد الطرود**
   - لا تشارك أكثر من 100 طرد في جلسة واحدة
   - استخدم الفلاتر لتقليل العدد

2. **أغلق الجلسات غير المستخدمة**
   - أنهِ الجلسات المكتملة
   - احذف الجلسات القديمة

3. **راقب عدد المشاركين**
   - العدد المثالي: 2-5 مشاركين
   - أكثر من 10 مشاركين قد يؤثر على الأداء

4. **استخدم الإنترنت السريع**
   - المزامنة الفورية تحتاج اتصال جيد
   - سرعة 1 Mbps على الأقل

---

## 🚀 الميزات المستقبلية

### قيد التطوير

- [ ] إرسال دعوات عبر البريد الإلكتروني
- [ ] إشعارات Push للتحديثات
- [ ] دردشة نصية داخل الجلسة
- [ ] مشاركة الموقع الجغرافي
- [ ] تصدير تقرير الجلسة
- [ ] نسخ احتياطي تلقائي للجلسات
- [ ] تكامل مع Google Calendar
- [ ] إحصائيات متقدمة ورسوم بيانية

---

## 📞 الدعم

### للمساعدة

- **البريد الإلكتروني**: support@swipex.app
- **التوثيق**: [swipex.app/docs](https://swipex.app/docs)
- **GitHub**: [github.com/swipex/swipex](https://github.com/swipex/swipex)

---

## 📄 الترخيص

© 2026 SwiPex Team - جميع الحقوق محفوظة.

---

**صُنع بـ ❤️ في الجزائر** 🇩🇿

**SwiPex Pro v4.0** - نظام الجلسات التعاونية
